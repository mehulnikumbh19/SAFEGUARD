[project]
name = "safeguard"
version = "1.0.0"
description = "Security control review workbench for access, logging, and cloud assessments"
requires-python = ">=3.12"

# Vercel auto-detects Flask from app.py; no custom entrypoint required.
