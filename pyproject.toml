[project]
name = "safeguard"
version = "1.0.0"
description = "Security control review workbench for access, logging, and cloud assessments"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "app:app"
